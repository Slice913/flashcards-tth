extends layout.pug

block content
    section#content
     h2= text
     if hint
      p 
       i Hint: #{hint}   
    a(href=`${id}?${sideToShow}`)= sideToDisplay
    p
    a(href='/cards') Generate Random Card